# SafePLC-Assist Box 西门子杯模块说明

本文档从模块角度说明 `safeplc_assist_box/` 目录中的核心文件，帮助西门子杯评审理解系统不是普通搜索引擎，而是带安全边界和证据复核能力的工业知识安全问答终端原型。

---

## 1. 模块总览

| 模块 | 作用 |
|---|---|
| `app_assist_box.py` | Streamlit 产品界面、安全边界面板、证据面板和本地图文证据显示 |
| `question_classifier_v11.py` | 问题分类与处理路径判断 |
| `safety_risk_guard_v11.py` | 工业安全风险判断 |
| `evidence_confidence_v11.py` | Evidence Confidence 计算 |
| `answer_evidence_checker_v11.py` | Answer-Evidence Check |
| `evidence_card_formatter.py` | 结构化证据卡片格式化 |
| `product_demo_mode.py` | 产品演示模式与演示场景支持 |
| `work_order_demo.py` | 工单式演示输出 |
| `run_v11_eval.py` | V1.1 评测运行脚本 |
| `run_v12_extended_eval.py` | V1.2 扩展评测运行脚本 |
| `testset_v11_basic.json` | V1.1 基础测试集 |
| `testset_v12_extended.json` | V1.2 扩展测试集 |
| `eval_report_v11.json` | V1.1 评测结果 |
| `eval_report_v12_extended.json` | V1.2 扩展评测结果 |
| `demo_cases.json` | 产品演示问题案例 |

---

## 2. 主流程

```text
用户问题
->
问题分类
->
安全风险判断
->
工业知识回答
->
Evidence Confidence
->
Answer-Evidence Check
->
结构化证据卡片
->
Streamlit 界面中的本地图文证据显示
```

---

## 3. 核心模块说明

### 3.1 Streamlit 产品界面

实现文件：

```text
app_assist_box.py
```

该模块提供产品化前端，包括：

- 问题输入
- 离线只读安全边界展示
- Evidence Confidence 面板
- Answer-Evidence Check 面板
- 结构化证据卡片面板
- 本地图文证据显示

### 3.2 问题分类

实现文件：

```text
question_classifier_v11.py
```

该模块用于判断用户问题类型，例如参数查询、网络连接问题、安全注意事项、维护辅助、高风险操作请求和需要澄清的问题。

### 3.3 安全风险判断

实现文件：

```text
safety_risk_guard_v11.py
```

该模块用于识别潜在不安全工业操作请求。系统不会提供真实 PLC 控制、写入、下载、启停、强制输出或绕过安全回路的可执行步骤。

### 3.4 Evidence Confidence

实现文件：

```text
evidence_confidence_v11.py
```

该模块估计当前回答是否有足够证据支持，典型输出包括 High、Medium、Low 和 Conflict。

### 3.5 Answer-Evidence Check

实现文件：

```text
answer_evidence_checker_v11.py
```

该模块检查回答中的关键声明是否被证据支持，典型输出包括 PASS、REVIEW 和 FAIL。

### 3.6 结构化证据卡片

实现文件：

```text
evidence_card_formatter.py
```

证据卡片包含 page、figure_id、source、title、module、parameter、snippet 和 confidence 等字段，用于提高回答可追溯性。

### 3.7 本地图文证据显示

主要实现文件：

```text
app_assist_box.py
```

当证据卡片包含页码或 `figure_id` 时，前端会尝试在本地演示环境中查找对应图文证据文件并显示。

示例：

```text
page: 641
figure_id: page_0641_visual
```

对应文件示例：

```text
page_0641.jpg
```

完整图文证据资产不包含在公开仓库中。

---

## 4. 技术边界

SafePLC-Assist Box 不连接真实 PLC，不接入 TIA Portal，不执行 PLC 通信、写入、下载、启停或控制动作，也不采集真实 IT / OT 网络数据。

系统定位是离线、只读的工业知识问答和证据复核原型，不替代西门子官方手册、现场安全规程或具备资质的工程师判断。
